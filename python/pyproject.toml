[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = [
  "F",   # pyflakes (real bugs)
  "E",   # syntax / indentation
  "W",   # whitespace
  "B",   # flake8-bugbear
  "A",   # shadowing builtins
]
ignore = [
  "E501",  # long lines
]
